# Architecture Drift Check

Generated by `scripts/check-architecture-drift.sh`. Advisory only - cross-checks docs/ARCHITECTURE.md's hand-written prose against the current knowledge graph. A name match is substring-based, not semantic, so treat every flag as a lead, not a verdict.

## Core Components (ARCHITECTURE.md §3) still present in the graph

| Component (ARCHITECTURE.md §3) | Found in current graph? |
|---|---|
| AgentService | yes |
| Provider Abstraction | yes |
| TUI Application | yes |
| PromptAnalyzer | yes |

All documented core components have a matching node in the current graph.

## Top god nodes (GRAPH_REPORT.md) mentioned in ARCHITECTURE.md

| God node | Mentioned in ARCHITECTURE.md? |
|---|---|
| `App` | yes |
| `LLMRequest` | yes |
| `PlanDocument` | yes |
| `test_app()` | no |
| `Config` | yes |
| `AgentService` | yes |
| `Provider` | yes |
| `ToolExecutionContext` | yes |
| `SqlitePool` | yes |
| `QwenProvider` | no |

**2 of the top 10 god nodes are never named in ARCHITECTURE.md** - each is either covered under a different name/section, or a candidate to add to §3.

