# Architecture Validation

Generated by `scripts/validate-architecture.sh`. Advisory only - nothing here fails a build; every finding needs a human look before acting on it (see caveats in each section).

## Unused Dependencies (cargo-machete)

cargo-machete flags these as unused. It does static text matching only, so it cannot see macro-injected imports or code gated behind a non-default feature - each is re-checked here with a whole-repo grep before you trust the flag:

| Dependency | grep hit in src/tests/benches? | Verdict |
|---|---|---|
| `arc-swap` | no | no reference found anywhere - candidate for removal |
| `async-openai` | no | no reference found anywhere - candidate for removal |
| `aws-sdk-bedrockruntime` | no | no reference found anywhere - candidate for removal |
| `base64` | yes | possibly used (behind a feature/macro) - verify manually |
| `bytes` | yes | possibly used (behind a feature/macro) - verify manually |
| `clap_complete` | no | no reference found anywhere - candidate for removal |
| `color-eyre` | no | no reference found anywhere - candidate for removal |
| `git2` | no | no reference found anywhere - candidate for removal |
| `ignore` | yes | possibly used (behind a feature/macro) - verify manually |
| `lsp-types` | no | no reference found anywhere - candidate for removal |
| `mime` | no | no reference found anywhere - candidate for removal |
| `parking_lot` | no | no reference found anywhere - candidate for removal |
| `pin-project` | no | no reference found anywhere - candidate for removal |
| `ratatui-image` | no | no reference found anywhere - candidate for removal |
| `shell-words` | no | no reference found anywhere - candidate for removal |
| `tokio-stream` | no | no reference found anywhere - candidate for removal |
| `tokio-util` | no | no reference found anywhere - candidate for removal |
| `tower-lsp` | no | no reference found anywhere - candidate for removal |
| `tree-sitter` | no | no reference found anywhere - candidate for removal |
| `tui-tree-widget` | no | no reference found anywhere - candidate for removal |
| `viuer` | no | no reference found anywhere - candidate for removal |

## Import Cycles & Orphan Modules (docs/graph/graph.json)

Built from `imports_from` edges between whole-file nodes only (not symbol-level calls/references, which cycle constantly in normal Rust code and aren't a useful signal here).

### Cycles (0 found)

Excludes same-directory `mod.rs` <-> submodule edges, which are structural to Rust's module system, not a design smell.

No import cycles detected among source files.

### Orphan Modules (44 graph-only candidates)

`imports_from` extraction misses some import styles - notably `use super::x::y` relative imports, confirmed by spot-check during Phase 2 (see e.g. `src/tui/highlight.rs`, imported by `src/tui/markdown.rs` via `use super::highlight::...` but not captured as an edge). Every graph-only candidate is therefore re-checked with a whole-repo grep for its module name before being called a real orphan:

| File | grep hit elsewhere? | Verdict |
|---|---|---|
| `src/db/repository/compaction.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/db/repository/file.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/db/repository/message.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/db/retry.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/agent/compaction.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/agent/error.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/pdf_context.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/anthropic.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/azure.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/factory.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/model_hints.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/ollama.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/ollama_models.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/openai.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/provider/retry.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/ask_user.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/bash.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/code_exec.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/context.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/doc_parser.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/edit.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/error.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/glob.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/grep.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/http.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/ls.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/notebook.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/plan_tool.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/plan_tool_security_tests.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/powershell.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/sandbox.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/skill.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/task.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/todo_write.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/web_fetch.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/llm/tools/web_search.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/logging.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/mcp/client.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/tui/error.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/tui/highlight.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/tui/markdown.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/tui/ollama_download.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/tui/plan_tests.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |
| `src/tui/runner.rs` | yes | referenced elsewhere - graph missed the edge, not orphaned |

**0 of 44** candidates found no reference either in the graph or via grep.

